class ContactList:
    def __init__(self):
        contacts = [] #a list of dictionaries, {Key: Name, Value: PhoneNumber} name will use .lower

    def add_contact({})
        pass

    def remove_contact(contact): #will remove a contact from the list by name
        pass
    
    def find_shared_contacts(ContactList): #accepts another contact list as an arguement, then returns a new list of dictionaries to indicate all the contacts that appear in both lists (exact same name and phone number)
        pass